打开Text_Editor，然后点sln文件就行了。

data和format文件放到D盘根目录下，或者你改main里的路径，都行。

main随便改，其他的改了记得做一下标记，以防我后来整合不小心删掉。

easyx安装在旁边，如果没有bits/stdc++.h的可以查一下怎么安装，文件本身我也放了。应该只有这两个需要，无法编译及时找我。

建议多用用，bug及时反馈，已知问题是[]这两个符号占位过小，不匹配字节数（占一个字节，但大小只有正常的一半），所以有这两个符号时，会导致sentence占位过多，然后显示有空白。很好改，但我没时间了。

